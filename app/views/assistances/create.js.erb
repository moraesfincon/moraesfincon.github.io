// after create contact

  <% if notice.present? %>
    var html = "<p> Chamado aberto sucesso!</p>";
    $(html).hide().appendTo(".alert").fadeIn(1000);
    $( '#new_assistance' ).each(function(){
        this.reset();
    });
  <% end %>
